# PhoneShop-NEW

## TL;DR

Start the server

```bash
cd server
npm install
npm start
```

Start the React client (Store)

```bash
cd client-react
npm install
npm start
```

Start the Angular client (Admin)

```bash
cd client-angular
npm install
ng serve --open
```